# Library Site

This is a library website.

Folder order:
- `pages/` for HTML files
- `assets/css/` for CSS files
- `assets/js/` for JavaScript files
- `assets/images/` for images
- `assets/media/` for video files